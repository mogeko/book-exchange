// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
  // See: https://vercel.com/docs/storage/vercel-postgres/using-an-orm#prisma
  url       = env("POSTGRES_PRISMA_URL") // uses connection pooling
  directUrl = env("POSTGRES_URL_NON_POOLING") // uses a direct connection
}

model Auth {
  id        String   @id @default(cuid())
  loginTime DateTime @default(now())
  password  String

  user      User   @relation(fields: [userEmail], references: [email])
  userEmail String @unique
}

model User {
  id        String   @id @default(cuid())
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  email     String   @unique
  name      String?
  avatar    String?

  authentication Auth?
  followings     Somebody[] @relation("Follow with")
  followees      Somebody[] @relation("Be followed")
  books          Owner[]
}

model Somebody {
  followee    User     @relation("Follow with", fields: [followeeId], references: [id])
  followeeId  String
  following   User     @relation("Be followed", fields: [followingId], references: [id])
  followingId String
  createdAt   DateTime @default(now())

  @@id([followeeId, followingId])
}

model Owner {
  owner     User     @relation(fields: [ownerId], references: [id])
  ownerId   String
  book      Book     @relation(fields: [bookId], references: [id])
  bookId    String
  createdAt DateTime @default(now())

  @@id([ownerId, bookId])
}

model Writer {
  id   String @id @default(cuid())
  name String

  artworks Author[]
}

model Author {
  writer   Writer @relation(fields: [writerId], references: [id])
  writerId String
  book     Book   @relation(fields: [bookId], references: [id])
  bookId   String

  @@id([writerId, bookId])
}

model Publisher {
  id   String @id @default(cuid())
  name String

  published Book[]
}

model Series {
  id          String   @id @default(cuid())
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
  name        String   @unique
  discription String?
  cover       String?

  books Book[]
}

model Book {
  id          String   @id @default(cuid())
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
  title       String
  discription String?
  isbn        String?  @unique
  cover       String?

  owners      Owner[]
  authors     Author[]
  publisher   Publisher? @relation(fields: [publisherId], references: [id])
  publisherId String?
  series      Series?    @relation(fields: [seriesId], references: [id])
  seriesId    String?
}
